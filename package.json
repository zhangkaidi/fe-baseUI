{
  "scripts": {
    "dev": "vuepress dev",
    "build": "vuepress build",
    "deploy": "gh-pages -d dist",
    "deploy:build": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "vuepress": "^1.5.2"
  }
}
